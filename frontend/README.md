# Study Center Frontend

Frontend приложение для учебного центра на React + TypeScript + Vite.

## 🚀 Быстрый старт

### Установка

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшн

```bash
npm run build
```

### Просмотр собранного приложения

```bash
npm run preview
```

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── components/          # Переиспользуемые компоненты
│   │   ├── Layout.tsx       # Основной layout
│   │   ├── Navbar.tsx       # Навигация с переключателем языков
│   │   ├── Footer.tsx       # Футер
│   │   ├── CourseCard.tsx   # Карточка курса
│   │   ├── ScheduleTable.tsx # Таблица расписания
│   │   ├── BookingModal.tsx # Модальное окно записи
│   │   └── PageTransition.tsx # Анимации переходов
│   ├── pages/              # Страницы приложения
│   │   ├── Home.tsx        # Главная страница
│   │   ├── Courses.tsx     # Список курсов
│   │   ├── CourseDetails.tsx # Детали курса
│   │   ├── Branches.tsx     # Филиалы
│   │   ├── Account.tsx     # Личный кабинет
│   │   └── Admin.tsx        # Админ-панель
│   ├── i18n/               # Локализация
│   │   ├── config.ts        # Конфигурация i18next
│   │   └── locales/         # Файлы переводов
│   │       ├── ru.json      # Русский
│   │       └── en.json      # Английский
│   ├── App.tsx             # Главный компонент
│   ├── main.tsx            # Точка входа
│   └── index.css           # Глобальные стили
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── tailwind.config.js
```

## 🎨 Технологии

- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик
- **Tailwind CSS** - Стилизация
- **React Router** - Маршрутизация
- **Framer Motion** - Анимации
- **react-i18next** - Локализация

## ✨ Особенности

- ✅ Полная локализация (RU/EN) с переключателем языков
- ✅ Плавные анимации переходов между страницами
- ✅ Адаптивный дизайн для всех устройств
- ✅ Accessibility атрибуты (ARIA)
- ✅ Микровзаимодействия (hover эффекты, анимации карточек)
- ✅ Современный UI в стиле Tailwind CSS

## 📄 Страницы

### Главная
- Hero секция
- USP (преимущества)
- Карусель курсов
- CTA секция

### Курсы
- Список всех курсов
- Фильтры (категория, цена, длительность)
- Карточки курсов с анимациями

### Страница курса
- Подробное описание
- Расписание занятий
- Отзывы студентов
- Кнопка записи

### Филиалы
- Карта филиалов (заглушка для интеграции)
- Список филиалов с контактами

### Личный кабинет
- Профиль пользователя
- Мои записи
- История платежей

### Админ-панель
- CRUD курсов
- Управление расписаниями
- Отчёты и статистика

## 🎯 Компоненты

### CourseCard
Карточка курса с анимацией при наведении и появлении.

### ScheduleTable
Таблица расписания с сортировкой по дням недели.

### BookingModal
Модальное окно для записи на курс с валидацией формы.

### Navbar
Навигация с мобильным меню и переключателем языков.

## 🌐 Локализация

Поддерживаются два языка:
- Русский (по умолчанию)
- English

Переключение языков доступно в навигации.

## 🎭 Анимации

Используется Framer Motion для:
- Переходов между страницами
- Появления элементов при скролле
- Hover эффектов на карточках
- Микровзаимодействий (кнопки, модальные окна)

## ♿ Accessibility

- ARIA атрибуты на всех интерактивных элементах
- Семантический HTML
- Навигация с клавиатуры
- Правильные роли и метки

## 📱 Адаптивность

Приложение полностью адаптивно:
- Mobile (< 640px)
- Tablet (640px - 1024px)
- Desktop (> 1024px)

## 🔧 Разработка

### Добавление нового перевода

1. Откройте `src/i18n/locales/ru.json` или `en.json`
2. Добавьте новый ключ и значение
3. Используйте в компоненте: `const { t } = useTranslation(); t('your.key')`

### Добавление новой страницы

1. Создайте файл в `src/pages/`
2. Добавьте маршрут в `src/App.tsx`
3. Оберните в `PageTransition` для анимаций

## 📝 Лицензия

ISC














