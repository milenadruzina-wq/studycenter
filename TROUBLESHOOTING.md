# Решение проблемы "Failed to fetch"

## Проблема
Ошибка "Failed to fetch" означает, что frontend не может подключиться к backend серверу.

## Решение

### 1. Убедитесь, что backend сервер запущен

Откройте отдельное окно терминала и выполните:

```bash
cd c:\Users\user\Desktop\studycenter
npm run dev
```

Вы должны увидеть:
```
✅ База данных успешно подключена!
🚀 Сервер запущен на порту 3000
📡 API доступен по адресу http://localhost:3000/api
```

### 2. Проверьте, что сервер работает

Откройте в браузере: `http://localhost:3000/health`

Должен вернуться JSON:
```json
{
  "status": "ok",
  "message": "Study Center API is running"
}
```

### 3. Проверьте URL в frontend

Убедитесь, что в `frontend/src/context/AuthContext.tsx` используется правильный URL:
```typescript
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';
```

### 4. Проверьте CORS настройки

В `src/server.ts` должно быть:
```typescript
app.use(cors({
  origin: process.env.FRONTEND_URL || "http://localhost:5173",
  credentials: true,
}));
```

### 5. Проверьте порты

- Backend должен работать на порту **3000**
- Frontend должен работать на порту **5173** (Vite по умолчанию)

### 6. Если проблема сохраняется

1. Проверьте консоль браузера (F12 → Console) на другие ошибки
2. Проверьте вкладку Network в DevTools - там должны быть запросы к `http://localhost:3000/api/auth/login`
3. Убедитесь, что нет блокировки файрволом
4. Попробуйте перезапустить оба сервера

## Быстрая проверка

Выполните в терминале:

```bash
# Проверка backend
curl http://localhost:3000/health

# Или в PowerShell
Invoke-WebRequest -Uri http://localhost:3000/health
```

Если команда не выполняется, значит сервер не запущен.












