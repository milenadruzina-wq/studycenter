# Быстрый старт - Решение проблемы ERR_CONNECTION_REFUSED

## Проблема
Ошибка `ERR_CONNECTION_REFUSED` означает, что backend сервер не запущен.

## Решение

### Шаг 1: Создайте файл .env (если его нет)

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=12345678
DB_DATABASE=studycenter

# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
```

### Шаг 2: Запустите backend сервер

Откройте **отдельное окно терминала** и выполните:

```bash
cd c:\Users\user\Desktop\studycenter
npm run dev
```

**ВАЖНО:** Оставьте это окно открытым! Сервер должен работать постоянно.

Вы должны увидеть:
```
✅ База данных успешно подключена!
🚀 Сервер запущен на порту 3000
📡 API доступен по адресу http://localhost:3000/api
```

### Шаг 3: Проверьте работу сервера

Откройте в браузере: **http://localhost:3000/health**

Должен вернуться JSON:
```json
{
  "status": "ok",
  "message": "Study Center API is running"
}
```

### Шаг 4: Запустите frontend (в другом окне терминала)

```bash
cd c:\Users\user\Desktop\studycenter\frontend
npm run dev
```

### Шаг 5: Войдите в систему

- Откройте браузер: **http://localhost:5173**
- Перейдите на страницу входа
- Username: `admin`
- Password: `admin201075`

## Важно!

**Backend и Frontend должны работать одновременно в разных окнах терминала:**

1. **Окно 1:** Backend (`npm run dev` в корне проекта)
2. **Окно 2:** Frontend (`npm run dev` в папке frontend)

## Если проблема сохраняется

1. Убедитесь, что PostgreSQL запущен
2. Проверьте, что база данных `studycenter` создана
3. Выполните: `npm run sync-db` для синхронизации БД
4. Выполните: `npm run create-admin` для создания админа












