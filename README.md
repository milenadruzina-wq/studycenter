# Study Center - Система управления учебным центром

Полнофункциональная система управления учебным центром с backend на Node.js + Express + TypeORM + PostgreSQL и frontend на React + TypeScript + Vite.

## 🚀 Быстрый старт

### Требования

- Node.js (версия 16 или выше)
- PostgreSQL (версия 12 или выше)
- npm или yarn

### Установка

1. **Клонируйте репозиторий и установите зависимости:**

```bash
# Backend зависимости
npm install

# Frontend зависимости
cd frontend
npm install
cd ..
```

2. **Создайте базу данных PostgreSQL:**

```bash
# Подключитесь к PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE studycenter;
```

3. **Создайте файл `.env` в корне проекта:**

```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=12345678
DB_DATABASE=studycenter

# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
```

4. **Создайте файл `.env` в папке `frontend` для настройки Google Maps:**

```env
# API URL
VITE_API_URL=http://localhost:3000/api

# Google Maps API Key (опционально)
# Получите ключ на https://console.cloud.google.com/google/maps-apis
# 1. Создайте проект в Google Cloud Console
# 2. Включите Maps JavaScript API
# 3. Создайте API ключ
# 4. Ограничьте ключ по HTTP referrer для безопасности
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
```

**Как получить Google Maps API ключ:**
1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите **Maps JavaScript API** в разделе "APIs & Services" > "Library"
4. Перейдите в "APIs & Services" > "Credentials"
5. Нажмите "Create Credentials" > "API Key"
6. Скопируйте ключ и добавьте его в `.env` файл
7. (Рекомендуется) Ограничьте ключ по HTTP referrer, добавив ваш домен

5. **Синхронизируйте базу данных:**

```bash
npm run sync-db
```

Эта команда создаст все необходимые таблицы в базе данных.

### Запуск проекта

#### Backend

**Режим разработки:**
```bash
npm run dev
```

Сервер будет доступен по адресу `http://localhost:3000`

**Продакшн режим:**
```bash
npm run build
npm start
```

#### Frontend

```bash
cd frontend
npm run dev
```

Frontend будет доступен по адресу `http://localhost:5173`

## 📊 Структура базы данных

### Сущности

- **Student** (Студенты)
  - id, firstName, lastName, email, phone, dateOfBirth, address
  - Связь с Group (многие к одному)
  - Связь с Grade (один ко многим)

- **Teacher** (Преподаватели)
  - id, firstName, lastName, email, phone, specialization, bio
  - Связь с Course (один ко многим)

- **Course** (Курсы)
  - id, name, description, durationHours, price
  - Связь с Teacher (многие к одному)
  - Связь с Group (один ко многим)

- **Group** (Группы)
  - id, name, maxStudents, startDate, endDate
  - Связь с Course (многие к одному)
  - Связь с Student (один ко многим)
  - Связь с Schedule (один ко многим)

- **Schedule** (Расписание)
  - id, dayOfWeek, startTime, endTime, classroom
  - Связь с Group (многие к одному)

- **Grade** (Оценки)
  - id, subject, score, comment, date
  - Связь с Student (многие к одному)

## 🔄 Синхронизация базы данных

### Автоматическая синхронизация

Автосинхронизация схемы БД включена по умолчанию (`synchronize: true` в `data-source.ts`). 
При запуске сервера TypeORM автоматически создаст или обновит таблицы в соответствии с моделями.

### Ручная синхронизация

Для ручной синхронизации БД используйте команду:

```bash
npm run sync-db
```

Эта команда:
- Подключается к базе данных
- Создает/обновляет все таблицы
- Создает внешние ключи и связи
- Закрывает соединение

**⚠️ Внимание:** В продакшн среде рекомендуется отключить автосинхронизацию и использовать миграции.

## 📡 API Endpoints

Полная документация API доступна в [API.md](API.md)

### Основные endpoints

- `GET /health` - Проверка работоспособности API
- `GET /api/students` - Список студентов
- `GET /api/teachers` - Список преподавателей
- `GET /api/courses` - Список курсов
- `GET /api/groups` - Список групп
- `GET /api/schedules` - Расписание
- `GET /api/grades` - Оценки

Все endpoints поддерживают полный CRUD (GET, POST, PUT, DELETE).

### Примеры запросов

**Получить все курсы:**
```bash
GET http://localhost:3000/api/courses
```

**Создать студента:**
```bash
POST http://localhost:3000/api/students
Content-Type: application/json

{
  "firstName": "Иван",
  "lastName": "Иванов",
  "email": "ivan@example.com",
  "phone": "+7 999 123 45 67",
  "groupId": 1
}
```

**Получить расписание группы:**
```bash
GET http://localhost:3000/api/schedules/group/1
```

## 🛠️ Технологии

### Backend

- **Express.js** - веб-фреймворк для Node.js
- **TypeORM** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **TypeScript** - типизированный JavaScript
- **CORS** - поддержка кросс-доменных запросов
- **Helmet** - безопасность HTTP заголовков
- **Morgan** - логирование HTTP запросов

### Frontend

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **Tailwind CSS** - стилизация
- **React Router** - маршрутизация
- **Framer Motion** - анимации
- **react-i18next** - локализация (RU/EN)

## 📁 Структура проекта

```
studycenter/
├── src/                    # Backend (Node.js + Express + TypeORM)
│   ├── entities/          # Модели данных (TypeORM)
│   │   ├── Student.ts
│   │   ├── Teacher.ts
│   │   ├── Course.ts
│   │   ├── Group.ts
│   │   ├── Schedule.ts
│   │   └── Grade.ts
│   ├── controllers/       # Контроллеры (бизнес-логика)
│   │   ├── student.controller.ts
│   │   ├── teacher.controller.ts
│   │   ├── course.controller.ts
│   │   ├── group.controller.ts
│   │   ├── schedule.controller.ts
│   │   └── grade.controller.ts
│   ├── routes/            # Маршруты API
│   │   ├── index.ts
│   │   ├── student.routes.ts
│   │   ├── teacher.routes.ts
│   │   ├── course.routes.ts
│   │   ├── group.routes.ts
│   │   ├── schedule.routes.ts
│   │   └── grade.routes.ts
│   ├── middleware/        # Middleware
│   │   └── errorHandler.ts
│   ├── data-source.ts     # Конфигурация TypeORM
│   ├── server.ts          # Точка входа сервера
│   ├── sync-db.ts         # Скрипт синхронизации БД
│   └── index.ts           # Утилита для проверки БД
├── frontend/              # Frontend (React + TypeScript + Vite)
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   │   ├── Layout.tsx
│   │   │   ├── Navbar.tsx
│   │   │   ├── Footer.tsx
│   │   │   ├── CourseCard.tsx
│   │   │   ├── ScheduleTable.tsx
│   │   │   ├── BookingModal.tsx
│   │   │   └── PageTransition.tsx
│   │   ├── pages/         # Страницы приложения
│   │   │   ├── Home.tsx
│   │   │   ├── Courses.tsx
│   │   │   ├── CourseDetails.tsx
│   │   │   ├── Branches.tsx
│   │   │   ├── Account.tsx
│   │   │   └── Admin.tsx
│   │   ├── i18n/          # Локализация
│   │   │   ├── config.ts
│   │   │   └── locales/
│   │   │       ├── ru.json
│   │   │       └── en.json
│   │   ├── App.tsx
│   │   └── main.tsx
│   └── package.json
├── package.json           # Backend зависимости и скрипты
├── API.md                 # Документация API
├── env.example.txt        # Пример переменных окружения
└── README.md
```

## 📝 Скрипты

### Backend скрипты

```bash
# Разработка (с автоперезагрузкой)
npm run dev

# Сборка проекта
npm run build

# Запуск в продакшн режиме
npm start

# Синхронизация базы данных
npm run sync-db
```

### Frontend скрипты

```bash
cd frontend

# Разработка
npm run dev

# Сборка для продакшн
npm run build

# Просмотр собранного приложения
npm run preview
```

## 🎨 Frontend

Frontend приложение находится в папке `frontend/`. Подробная документация: [frontend/README.md](frontend/README.md)

### Особенности Frontend

- ✅ React 18 + TypeScript + Vite
- ✅ Tailwind CSS для стилизации
- ✅ Локализация (RU/EN) с react-i18next
- ✅ Анимации с Framer Motion
- ✅ Адаптивный дизайн
- ✅ Accessibility (ARIA атрибуты)
- ✅ Все требуемые страницы и компоненты

### Страницы Frontend

- **Главная** - Hero секция, USP, карусель курсов, CTA
- **Курсы** - Список с фильтрами, карточки курсов
- **Страница курса** - Описание, расписание, отзывы, запись
- **Филиалы** - Карта и список филиалов
- **Личный кабинет** - Профиль, записи, история платежей
- **Админ-панель** - CRUD курсов, расписаний, отчёты

## 📝 Примеры использования API

### Создание курса с преподавателем

```bash
# 1. Создать преподавателя
POST /api/teachers
{
  "firstName": "Иван",
  "lastName": "Петров",
  "email": "ivan@example.com",
  "specialization": "Программирование"
}

# 2. Создать курс
POST /api/courses
{
  "name": "Основы программирования",
  "description": "Изучение основ",
  "durationHours": 120,
  "price": 25000,
  "teacherId": 1
}

# 3. Создать группу
POST /api/groups
{
  "name": "Группа A",
  "maxStudents": 20,
  "startDate": "2024-02-01",
  "endDate": "2024-06-01",
  "courseId": 1
}
```

### Запись студента на курс

```bash
# 1. Создать студента
POST /api/students
{
  "firstName": "Анна",
  "lastName": "Смирнова",
  "email": "anna@example.com",
  "phone": "+7 999 345 67 89",
  "groupId": 1
}

# 2. Добавить оценку
POST /api/grades
{
  "subject": "Программирование",
  "score": 5.0,
  "comment": "Отлично",
  "date": "2024-01-15",
  "studentId": 1
}
```

## 🔐 Безопасность

- Не коммитьте файл `.env` в репозиторий
- Используйте сильные пароли в продакшн среде
- Отключите автосинхронизацию в продакшн и используйте миграции
- Helmet настроен для защиты HTTP заголовков
- CORS настроен для безопасных кросс-доменных запросов

## 🐛 Отладка

### Проверка подключения к БД

```bash
npm run sync-db
```

### Просмотр логов

Сервер автоматически логирует все HTTP запросы через Morgan.

### Проверка работоспособности API

```bash
GET http://localhost:3000/health
```

## 📄 Лицензия

ISC

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.
