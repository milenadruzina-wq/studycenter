# 🚀 Инструкция по запуску сервера

## Быстрый запуск Backend сервера

### Способ 1: Через PowerShell (рекомендуется)

1. Откройте PowerShell в корневой папке проекта (`c:\Users\user\Desktop\studycenter`)
2. Выполните команду:
```powershell
npm run dev
```

### Способ 2: Автоматический запуск

Запустите скрипт `check-server.ps1`:
```powershell
.\check-server.ps1
```

Скрипт автоматически проверит, запущен ли сервер, и запустит его при необходимости.

## Проверка работы сервера

После запуска сервера вы должны увидеть в консоли:
```
Server is running on port 3000
Database connected successfully
```

## Решение проблем

### Ошибка: `ERR_CONNECTION_REFUSED`

1. **Убедитесь, что сервер запущен:**
   - Проверьте, есть ли окно PowerShell с запущенным `npm run dev`
   - Если нет - запустите сервер командой `npm run dev`

2. **Проверьте порт 3000:**
   ```powershell
   netstat -ano | findstr :3000
   ```
   Должна быть строка со статусом `LISTENING`

3. **Убейте процессы на порту 3000 (если нужно):**
   ```powershell
   Stop-Process -Name node -Force
   ```
   Затем запустите сервер заново

### Ошибка: База данных не подключена

1. Убедитесь, что PostgreSQL запущен
2. Проверьте настройки в файле `.env`
3. Убедитесь, что база данных `studycenter` создана

## Автоматический запуск при старте системы

Вы можете настроить автоматический запуск сервера при загрузке Windows, добавив задачу в Планировщик заданий Windows.








